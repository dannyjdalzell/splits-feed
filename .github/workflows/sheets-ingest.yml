name: Sheets → Twitter ingest (every 15m, hands-off)

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: sheets-ingest
  cancel-in-progress: true

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install pandas

      - name: Fetch published Google Sheet CSV
        env:
          SHEETS_CSV_URL: ${{ secrets.SHEETS_CSV_URL }}
        run: |
          test -n "$SHEETS_CSV_URL" || { echo "SHEETS_CSV_URL secret missing"; exit 1; }
          mkdir -p ./sources/sheets/twitter
          # cache-buster so Google doesn't hand us a stale copy
          curl -fsSL "$SHEETS_CSV_URL&no_cache=$(date +%s)" -o ./sources/sheets/twitter/tweets.csv
          test -s ./sources/sheets/twitter/tweets.csv || { echo "empty tweets.csv"; exit 1; }
          echo "tweets.csv lines:" $(wc -l < ./sources/sheets/twitter/tweets.csv)
          head -2 ./sources/sheets/twitter/tweets.csv || true

      - name: Run Twitter ingest
        run: |
          python scripts/ingest_twitter_csv.py \
            --csv  ./sources/sheets/twitter/tweets.csv \
            --dict ./dictionaries \
            --out  ./audit_out/twitter_resolved.csv

      - name: Quick debug (columns/first rows)
        run: |
          python - <<'PY'
          import pandas as pd
          df=pd.read_csv('audit_out/twitter_resolved.csv')
          print("twitter_resolved rows:", len(df))
          print("cols:", list(df.columns))
          print(df.head(3).to_string())
          PY

      - name: Commit updates
        run: |
          git config user.name  "splits-bot"
          git config user.email "actions@users.noreply.github.com"
          # force-add in case .gitignore still blocks
          git add -f audit_out/twitter_resolved.csv sources/sheets/twitter/tweets.csv || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "ci: refresh twitter_resolved from Sheets [skip ci]"
            git push
          fi
